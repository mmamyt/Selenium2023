package locators;

public class Review {


    public static void main(String[] args) {


        // 09/26/2023 Tue.
        // DriverManager => set up the browser
        //  get() => opens url
        //  getTitle() => gets page title
        //  navigate().To() => opes a url
        // driver.manage().window().maximize(); => maximizes the page
        // quit() => closes all opens tabs
        // close() => closes the current open window
        // click () => clicks the element
        // submit() => clicks the element
        //    isDisplayed() => checks if an element is displayed
        //    isSelected() => checks if radio or checkboxes is selected
        //    isEnabled() => checks if an element is enabled
        //    getCurrentURL() => it gets current url
        //    findElement() => it help to find an element
        //    findElements() => it help to find an elements
        //    navigate().back()
        //    navigate().forward()
        //    sendKeys() => enters data into input boxes
        // clear() => clears a text from input box
        // refresh() => refreshes the page
        // xpath, css, id, name, class....
        // getText() => is gets the text from an element
        // Select class => it helps with dropdowns
        //    : selectByVisibleText(), selectByValue(), selectByIndex()
        //    : deselectByVisibleText(), deselectByValue(), deselectByIndex(), deselectAll()
        // Thread.sleep() => pauses the code for [time]
        // Press.KEY from keyboard
        // WebElement
     //======9/27/23=============
        //Actions class:
        // moveToElement().perform() => moves mouse to element
        // doubleClick()
        // click() with action class
        // dragAndDrop() => helps us to move an element to another location
        // sendKeys(Keys.END) => scroll to the bottom of the screen
        // sendKeys(Keys.HOME) => scroll to the beginning/top of the screen
        // contextClick() => right Click
        //



























    }


















}
